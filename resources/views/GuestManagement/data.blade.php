<div>
    @if(isset($guests))
{{dd($guests)}}

<!-- @foreach($guests as $guest)
    <div>
        <p>{{$guest->max}}</p>
        <p>{{$guest->adult_guest}}</p>
        <p>{{$guest->child_guest}}</p>
        <p>{{$guest->age_of}}</p>
    </div>
@endforeach -->
@endif
</div>